{
  "name": "fixing-vertec-with-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reveal.js deck for a Zuhlke Camp talk on fixing Vertec with AI.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check": "npm run build"
  },
  "dependencies": {
    "reveal.js": "^6.0.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^8.0.13"
  }
}
